/// <reference types="vite/client" />

/** Injected by vite.config.ts: short commit hash and build instant (ISO 8601); shown in the viewer's local time. */
declare const __BUILD_COMMIT__: string;
declare const __BUILD_TIME__: string;
